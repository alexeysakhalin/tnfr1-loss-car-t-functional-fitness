# Reference figures

This directory is reserved for checksum-pinned reference panels generated by
the documented release workflows. Each retained panel must have a corresponding
entry in `SHA256SUMS` and an archived numerical QC record.
